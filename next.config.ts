import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Hide the floating Next.js dev-tools badge — it sits on top of the UI during demos.
  devIndicators: false,
};

export default nextConfig;
